import(lattice)
importFrom(stats, aggregate, na.omit, time, start, end)
import(sp)
import(spacetime)
import(rgdal)
import(maptools)

export(traj_reader,
	hourly_stats,
  STItoSTTDF,
  STTDFtoSpatialLines,
  STItoSpatialLines,
  summary.STI,
  crop,
  aggregate
)

exportMethods(
	plot
)

#S3method(as.data.frame, STF)
