import(sp)
import(spacetime)
import(lattice)
importFrom(stats, aggregate, na.omit, time, start, end)

export(traj_reader,
	toSTTDF,
	speed_cal,
	hourly_stats,
  STItoSTTDF
)

#S3method(as.data.frame, STF)
