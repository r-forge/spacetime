import(sp)
import(spacetime)
import(lattice)
importFrom(stats, aggregate, na.omit, time, start, end)
#importFrom(zoo, na.locf, na.spline, na.approx)

export(traj_reader,
	toSTTDF,
	speed_cal,
	hourly_stats
)

#S3method(as.data.frame, STF)
